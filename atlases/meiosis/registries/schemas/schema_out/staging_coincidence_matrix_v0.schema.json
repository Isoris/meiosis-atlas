{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "staging_coincidence_matrix_v0.schema.json",
  "title": "staging_coincidence_matrix_v0 — loose capture of one coincidence_matrix TSV",
  "description": "Staging payload for a per-(chrom × interval_a × interval_b) coincidence TSV. Captures columns + rows verbatim; promote to coincidence_matrix_v1 via the normalize_coincidence_matrix action once the producer's column set is stable.",
  "type": "object",
  "required": ["columns", "rows"],
  "additionalProperties": true,
  "properties": {
    "scope":   { "type": "string" },
    "source":  { "type": "string" },
    "columns": { "type": "array", "items": { "type": "string" } },
    "rows":    { "type": "array", "items": { "type": "object", "additionalProperties": true } },
    "n_rows":  { "type": "integer", "minimum": 0 }
  }
}
