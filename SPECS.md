# SPECS — meiosis-atlas master index

Cross-cutting index of every specification in this repo. Mirrors the
`inversion-atlas/SPECS.md` convention.

## Folder convention

```
specs_todo/   — design backlog (authored, not yet implemented)
specs_done/   — shipped (implementation matches the SPEC)
```

**Rule**: a SPEC never gets deleted. When code ships, move the SPEC
from `specs_todo/` to `specs_done/`, update its status line, and add an
`Implemented in:` block at the top.

## Shipped — `specs_done/`

| SPEC | what it covers | implementation |
|------|----------------|----------------|
| [SPEC_tract_classifications_adapter.md](specs_done/SPEC_tract_classifications_adapter.md) | IN + OUT JSON adapters for ngsTracts STEP_TRC_01 output (import_tract_classifications → staging_tract_classifications_v0; normalize_tract_classifications → tract_classifications_v1). Type coercion, summary block, `'-'` null sentinel. | [atlases/meiosis/registries/](atlases/meiosis/registries/) (12 files: dispatcher + 2 runners + 2 extractors + 4 schemas + 2 registries + smoke test) |
| [SPEC_chromosome_meiosis_events_adapter.md](specs_done/SPEC_chromosome_meiosis_events_adapter.md) | Second adapter pair: per-(chrom × dyad) CO/DCO/NCO event counts. Reuses the shared `import_tsv` runner from the first adapter; adds 2 schemas + 2 extractors + 1 normalize runner. Derives co_per_mb / dco_per_mb when the producer omits them. Karyotype-stratified rows (homA/het/homB) drive the intrachromosomal-effect view. **Unblocks** [SPEC_crossovers_page.md](specs_todo/SPEC_crossovers_page.md). | extends [atlases/meiosis/registries/](atlases/meiosis/registries/) — 7 new files + smoke-test extension |
| [SPEC_nco_page.md](specs_done/SPEC_nco_page.md) | `nco` hub page consuming `tract_classifications_v1` envelopes. Status badge + 4 views (per-dyad / length histogram / per-chrom / in-vs-out crosstab) + TSV export. The headline MOSAIC_SHORT × yes highlight. | [atlases/meiosis/pages/hub/nco.{html,js}](atlases/meiosis/pages/hub/) + [css/pages/nco.css](atlases/meiosis/css/pages/nco.css) + [shared/api_client.js](atlases/meiosis/shared/api_client.js) + 36-assertion smoke |
| [SPEC_crossovers_page.md](specs_done/SPEC_crossovers_page.md) | `crossovers` hub page consuming `chromosome_meiosis_events_v1`. Status badge + 4 views (count / rate_per_mb / position-stub / karyo_strat). The karyo_strat view's CO_rate(het) / CO_rate(non-het) cells < 0.7 → `var(--bad)` (manuscript-grade intrachromosomal-effect signal). | [atlases/meiosis/pages/hub/crossovers.{html,js}](atlases/meiosis/pages/hub/) + [css/pages/crossovers.css](atlases/meiosis/css/pages/crossovers.css) + ~30-assertion JS smoke |
| [SPEC_local_inv_controls_adapter.md](specs_done/SPEC_local_inv_controls_adapter.md) | Third adapter pair: per-(tested chrom × local inversion) covariate matrix; filtered subset of inversion_candidates.v1. Reuses the shared `import_tsv` runner. Derives length_bp from end - start + 1; clamps out-of-range frequencies to null; drops rows missing tested_chrom / inversion_id / start_bp. Build-order step 1 for the interchromosomal page. | extends [atlases/meiosis/registries/](atlases/meiosis/registries/) — 7 new files + smoke-test extension (6 new assertions) |
| [SPEC_family_aware_permutation_design_adapter.md](specs_done/SPEC_family_aware_permutation_design_adapter.md) | Fourth adapter pair: per-(focal_inversion × parent) null-model assignments. One row per parent under each focal inversion with `permutation_block` (typically family_id). Drops rows missing any of {focal_inversion_id, parent_id, family_id, karyotype, permutation_block}; unknown karyotype enums also drop. Computes `n_singleton_blocks` for the consumer's degeneracy diagnostic. Build-order step 4 for the interchromosomal page. | extends [atlases/meiosis/registries/](atlases/meiosis/registries/) — 7 new files + smoke-test extension (6 new assertions) |
| [SPEC_coincidence_matrix_adapter.md](specs_done/SPEC_coincidence_matrix_adapter.md) | Fifth adapter pair: per-(chrom × interval_a × interval_b) coefficient of coincidence `C = r_ab / (r_a · r_b)`. Derives c when producer omits it (and r_a · r_b > 0); flags rows with c > params.neg_interference_threshold (default 3.0) as artefact-likely; drops rows missing chrom / interval_a_id / interval_b_id. Supports both un-stratified (relatedness `coincidence` page) and karyotype-stratified (interchromosomal page) modes. **Final unblock for SPEC_interchromosomal_page.md.** | extends [atlases/meiosis/registries/](atlases/meiosis/registries/) — 7 new files + smoke-test extension (7 new assertions) |
| [SPEC_interchromosomal_page.md](specs_done/SPEC_interchromosomal_page.md) | **HEADLINE.** `interchromosomal` hub page consuming all 4 adapter envelopes. Real stats: Welch's t on per-parent CO rates het vs non-het, family-aware permutation null (seeded RNG; shuffles karyotype labels within `permutation_block`), BH + Bonferroni across off-focal chrom tests, p-BH < α highlight. **No fake data in normal sessions** — empty-state when envelopes missing; opt-in demo mode via `?demo=1` or `localStorage.atlasDemoMode=1` loads synthetic envelopes with a designed-significant cell for previews. | [atlases/meiosis/pages/hub/interchromosomal.{html,js}](atlases/meiosis/pages/hub/) + [interchromosomal/_stats.js + _demo.js](atlases/meiosis/pages/hub/interchromosomal/) + [css/pages/interchromosomal.css](atlases/meiosis/css/pages/interchromosomal.css) + ~30-assertion JS smoke against DEMO_ENVELOPES |
| [SPEC_upstream_dependencies.md](specs_done/SPEC_upstream_dependencies.md) | Separates `biomod_status` (compute implemented?) from `runnable` (inputs available?). New `cross_atlas_inputs` config block declares each upstream producer (`relatedness_atlas` — the principal blocker via family structure; `inversion_atlas`; `ngsTracts`; `ngsPedigree`) + per-analysis dependency. Generator emits 6th forwarding file `upstream_dependencies.jsonl` (9 rows, runnable 0/9) and stamps `blocked_on` + `runnable` onto every module row — so the catalogue brain can show WHY a `ready` chain can't dispatch. Headline interchromosomal chain `blocked_on: …, relatedness_atlas, inversion_atlas`. Unblocking = flip `available: true` + re-run `make ship`. Smoke-tested (blocked_on ⊆ upstream_sources; runnable ⇔ empty; module union consistency). | [catalogue_outbound_config.json](atlases/meiosis/registries/catalogue_outbound_config.json) + [generate_catalogue_outbound.py](atlases/meiosis/registries/generate_catalogue_outbound.py) + [test_catalogue_outbound.py](atlases/meiosis/registries/test_catalogue_outbound.py) |
| [SPEC_nco_per_candidate_v2_chain.md](specs_done/SPEC_nco_per_candidate_v2_chain.md) | v2 fan-out of the NCO chain: per-candidate Fisher + BH/Bonferroni across the inversion candidate set. Multi-source dispatch (`tracts_layer_id` + cross-atlas `candidates_layer_id`); shape-tolerant on the candidates envelope (`payload.candidates` OR `payload.inversions`). Skipped flag for candidates missing `{chrom, start_bp, end_bp}`. New action `compute_nco_per_candidate_enrichment`, new layer `nco_per_candidate_enrichment_v1`, new module `meiosis_nco_per_candidate_test` (ready). 40-assertion smoke (crosstab cells + boundary overlap + chrom restriction + missing-coord drops + designed-significant fixture INV_SIG OR=42 p=0.009 + BH/Bonferroni monotonicity + runner round-trip + cross-atlas envelope-key tolerance). Catalogue payload: 13/12/12/12/6 rows. Makefile `PY_TESTS`/`JS_TESTS` switched to recursive `find` glob — future test files land without Makefile edits. | [atlases/meiosis/registries/runners/{meiosis_nco_per_candidate.py, compute_nco_per_candidate.py}](atlases/meiosis/registries/runners/) + [extractors/normalize_nco_per_candidate_result.py](atlases/meiosis/registries/extractors/) + 2 schemas + registry updates + [Makefile](Makefile) tightening |
| [SPEC_chain_dispatch_wirings_co_ic.md](specs_done/SPEC_chain_dispatch_wirings_co_ic.md) | Crossovers page wired with opt-in "Run on server (karyo_strat only)" checkbox dispatching `compute_intrachromosomal_co_karyotype_effect` + new `renderServerKaryoStrat` for the typed `intrachromosomal_co_effect_v1` envelope (flagged ratio cells red, excluded-reason notes preserved). Interchromosomal HEADLINE page wired with "Run on server" checkbox dispatching `compute_interchromosomal_inversion_effect` (multi-source via named layer_ids; existing `renderResultTable` reused — server payload matches browser shape by design; demo mode auto-skips since synthetic envelopes have no server registration). Both fall back transparently to the browser path on dispatch failure with warn-coloured status badge. +8 crossovers smoke assertions; `make smoke` 11/11 green. | [atlases/meiosis/pages/hub/crossovers.{html,js}](atlases/meiosis/pages/hub/) + [atlases/meiosis/pages/hub/interchromosomal.{html,js}](atlases/meiosis/pages/hub/) + [css/pages/crossovers.css](atlases/meiosis/css/pages/crossovers.css) + [css/pages/interchromosomal.css](atlases/meiosis/css/pages/interchromosomal.css) |
| [SPEC_dispatch_and_makefile.md](specs_done/SPEC_dispatch_and_makefile.md) | End-to-end closure: `Makefile` for one-shot catalogue regen + smoke + tarball + atlas-core push (`make ship`, `make catalogue-push ATLAS_CORE_REPO=...`); new `runAction()` + `dispatchMeiosisChain()` helpers in `shared/api_client.js` (auto/strict modes, chain-name → action-type resolution, ApiError preservation); NCO page wired with an opt-in "Run on server" checkbox that dispatches `compute_nco_inside_vs_outside_inversion` and renders the typed result envelope (auto fallback to browser compute on dispatch failure, with a status badge). 22-assertion dispatch smoke + 7 new NCO `renderServerResult` assertions; `make smoke` runs all 11 suites green. | [Makefile](Makefile) + [atlases/meiosis/shared/api_client.js](atlases/meiosis/shared/api_client.js) + [atlases/meiosis/shared/test_api_client_dispatch.js](atlases/meiosis/shared/) + [atlases/meiosis/pages/hub/nco.{html,js}](atlases/meiosis/pages/hub/) + [css/pages/nco.css](atlases/meiosis/css/pages/nco.css) |
| [SPEC_interchromosomal_effect_chain_module.md](specs_done/SPEC_interchromosomal_effect_chain_module.md) | **HEADLINE chain promotion** — third and final chain bloc out of browser JS. Multi-source dispatch (events + controls + design envelopes), family-aware permutation null (mulberry32 PRNG ported 1:1 from JS for cross-path determinism), BH + Bonferroni across off-focal-chrom tests, focal-chrom row reported but excluded from alpha control. Catalogue brain dispatches via `POST /api/actions {type: 'compute_interchromosomal_inversion_effect', ...}`. 55-assertion smoke (mulberry32 vs canonical vector + family-aware-permutation block invariant over 200 trials + BH classic-example values + orchestrator end-to-end with designed-significant fixture + seeded determinism + 3-envelope workspace round-trip). **All chain modules now `ready`; zero stale chains remain.** | [atlases/meiosis/registries/runners/{meiosis_interchromosomal_effect.py, compute_interchromosomal_effect.py}](atlases/meiosis/registries/runners/) + [extractors/normalize_interchromosomal_effect_result.py](atlases/meiosis/registries/extractors/) + 2 schemas + registry updates |
| [SPEC_intrachromosomal_co_chain_module.md](specs_done/SPEC_intrachromosomal_co_chain_module.md) | Second chain bloc promoted from browser JS to a server-side biomod. Hand-rolled Welch's t with t-CDF via Lentz incomplete beta (no scipy). Per-chromosome karyotype split (het vs homA∪homB) + rate_ratio < flag_threshold flag. Catalogue brain dispatches via `POST /api/actions {type: 'compute_intrachromosomal_co_karyotype_effect', ...}`. 44-assertion smoke (t-CDF correctness + Welch internal consistency + karyotype split + payload shape + threshold logic + full runner+extractor round-trip in temp workspace). | [atlases/meiosis/registries/runners/{meiosis_intrachromosomal_co.py, compute_intrachromosomal_co.py}](atlases/meiosis/registries/runners/) + [extractors/normalize_intrachromosomal_co_result.py](atlases/meiosis/registries/extractors/) + 2 schemas + registry updates |
| [SPEC_nco_enrichment_chain_module.md](specs_done/SPEC_nco_enrichment_chain_module.md) | First chain bloc promoted from browser JS to a server-side biomod module. Hand-rolled Fisher exact (no scipy) + crosstab + chain action runner + extractor + 2 schemas + 2 registry entries. Catalogue brain can now dispatch via `POST /api/actions {type: 'compute_nco_inside_vs_outside_inversion', ...}`. Module row flips from `stale: "promotion_from_browser_js"` to `biomod_status: "ready"` with a new `dispatch_action` field. 37-assertion smoke (Fisher correctness vs scipy reference + crosstab + payload shape + full runner+extractor round-trip in temp workspace). | [atlases/meiosis/registries/runners/{meiosis_nco_enrichment.py, compute_nco_enrichment.py}](atlases/meiosis/registries/runners/) + [extractors/normalize_nco_enrichment_result.py](atlases/meiosis/registries/extractors/) + 2 schemas + actions/extractors registry updates |
| [SPEC_workflows_page.md](specs_done/SPEC_workflows_page.md) | `workflows` hub page: user-facing companion to the catalogue forwarding payload. Cross-joins the four `catalogue_outbound/*.jsonl` files; renders status table with bloc-status inference (ready / stale / contract-only), kind + status filters, TSV export, and in-page validation of atlas-core's three hard constraints. Fail-soft on fetch errors. | [atlases/meiosis/pages/hub/workflows.{html,js}](atlases/meiosis/pages/hub/) + [css/pages/workflows.css](atlases/meiosis/css/pages/workflows.css) + 36-assertion smoke |
| Per-candidate page wire-up 2026-05-20 (covered by [SPEC_meiosis_atlas_pages.md](specs_todo/SPEC_meiosis_atlas_pages.md); no per-page SPEC) | `crossovers_per_candidate` + `nco_per_candidate` pages wired with real SVG renderers. CO page: 3 views (sex-specific ideogram with span-band + telomere curve per sex + optional PRDM9 logo from `prdm9_motif.pwm`). NCO page: 2 views (NCO/GC tract ideogram + tract telomere curve per kind). Mode-B probe feeds the renderer when a per-candidate envelope resolves; static mockup remains until then. **No fake data in normal sessions** — demo banner activates via `?demo=1` / `localStorage.atlasDemoMode=1`. | new files: [`_per_candidate_helpers.js`](atlases/meiosis/pages/hub/_per_candidate_helpers.js); [`crossovers_per_candidate/_render.js` + `_demo.js`](atlases/meiosis/pages/hub/crossovers_per_candidate/); [`nco_per_candidate/_render.js` + `_demo.js`](atlases/meiosis/pages/hub/nco_per_candidate/); 2 JS smoke tests covering binSmooth, relTelomereDist, sequenceLogo, ideogram/curve renderers against the demo payloads |

## Backlog — `specs_todo/`

| SPEC | what it covers | status |
|------|----------------|--------|
| [SPEC_meiosis_atlas_pages.md](specs_todo/SPEC_meiosis_atlas_pages.md) | Master page-set overview: the 5 hub pages + 3 missing builders + cross-atlas dependencies | All 5 hub pages now wired (nco, crossovers, interchromosomal, crossovers_per_candidate, nco_per_candidate). Backlog effectively empty pending real cluster-side data for the per-candidate envelopes (`crossover_track`, `nco_gc_track`, `prdm9_motif` — all CONTRACT-ONLY in round 1). |
| [SPEC_meiosis_workflow_catalogue.md](specs_todo/SPEC_meiosis_workflow_catalogue.md) | Forwarding payload (4 JSONL files: module / analysis / analysis_modes / layer registries) for atlas-core's Catalogue page 4. Mirrors the popstats / unified-ancestry registration shape: one bloc per atomic stat + one per CHAIN (nco_in_vs_out, intrachromosomal_co_karyotype_effect, interchromosomal_inversion_effect HEADLINE). | Drafted under [`atlases/meiosis/registries/catalogue_outbound/`](atlases/meiosis/registries/catalogue_outbound/). Smoke-tested (12 modules / 11 analyses / 11 modes / 11 layers, 0 constraint violations). Awaiting atlas-core ingestion + server-side promotion of three chain test modules currently inlined in browser JS. |

## Cross-atlas dependencies

The meiosis_atlas is registered in `atlas-core/toolkit_registries/relatedness/01_registry/atlases.jsonl` with `depends_on_atlases: [relatedness_atlas, inversion_atlas]`. Per that registry, primary products are: `chromosome_meiosis_events.v1`, `gene_conversion_tracts.v1`, `coincidence_matrix.v1`, `local_inv_controls.v1`, `family_aware_permutation_design.v1`, `inversion_meiosis_effects.v1`.

## Paired analysis repo

[`ngsTracts`](https://github.com/Isoris/ngsTracts) — classifier for parent–offspring haplotype departures (NCO / CO / DCO / MOSAIC_* / AMBIG / LOW_CONFIDENCE). The meiosis-atlas is the consumer / browser UI; ngsTracts is the producer.
